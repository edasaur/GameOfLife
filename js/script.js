var canvas = document.getElementById("map");
/*for (var x = 0.5; x < canvas.width; x += 2) {
	context.moveTo(x,0);
	context.lineTo(x,canvas.height);
}

for (var y = 0.5; y < canvas.height; y+=2) {
	context.moveTo(0,y);
	context.lineTo(canvas.width, y)
}*/
